language: python
python:
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"

install:
    - pip install pyserial numpy bitstring mido coverage

script:
    - nosetests -v -d --cover-erase --with-coverage --cover-package=cantoolz

notifications:
    email: false
